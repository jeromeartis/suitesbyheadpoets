# If a dependency (or one of its own dependencies) declares it needs a newer
# Node.js than you have installed, this stops npm from treating that as a
# hard error. It still installs fine on Node 18+; if something truly breaks
# at runtime due to a Node version mismatch, upgrading Node (nvm install --lts)
# is the real fix.
engine-strict=false
